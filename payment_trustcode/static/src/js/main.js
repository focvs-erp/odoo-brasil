"use strict";
odoo.define(
    "payment_trustcode.PaymentRedirect",
    [],
    function (require) {
        require("web.dom_ready");

        var ajax = require("web.ajax");

        console.log(e);
        alert("Hello");

        btn.click(_onBtnClick);
    }
);
